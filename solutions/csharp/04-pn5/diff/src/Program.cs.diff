@@ -1,14 +1,18 @@
 class Program
 {
     static void Main()
     {
         while (true)
         {
             Console.Write("$ ");

             // Wait for user input
             var command = Console.ReadLine();
+            if (command == "exit")
+            {
+                break;
+            }
             Console.WriteLine($"{command}: command not found");
         }
     }
 }
