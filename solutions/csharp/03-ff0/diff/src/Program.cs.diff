@@ -1,11 +1,14 @@
 class Program
 {
     static void Main()
     {
-        Console.Write("$ ");
+        while (true)
+        {
+            Console.Write("$ ");

-        // Wait for user input
-        var command = Console.ReadLine();
-        Console.WriteLine($"{command}: command not found");
+            // Wait for user input
+            var command = Console.ReadLine();
+            Console.WriteLine($"{command}: command not found");
+        }
     }
 }
