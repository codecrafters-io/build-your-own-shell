@@ -1,5 +1,7 @@
-$stdout.write("$ ")
-$stdout.flush
-command = $stdin.gets
-command = command ? command.chomp : ""
-$stdout.puts("#{command}: command not found")
+loop do
+  $stdout.write("$ ")
+  $stdout.flush
+  command = $stdin.gets
+  command = command ? command.chomp : ""
+  $stdout.puts("#{command}: command not found")
+end
