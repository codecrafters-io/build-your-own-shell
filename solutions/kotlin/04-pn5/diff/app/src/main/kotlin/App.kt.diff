@@ -1,8 +1,11 @@
 fun main() {
     while (true) {
         print("$ ")
         System.out.flush()
         val command = readLine() ?: return
+        if (command == "exit") {
+            break
+        }
         println("$command: command not found")
     }
 }
