@@ -1,5 +1,11 @@
 import gleam/io
+import gleam/erlang
+import gleam/string

 pub fn main() {
   io.print("$ ")
+  let assert Ok(command) = erlang.get_line("") 
+  
+  let cmd = string.trim(command)
+  io.println(cmd <> ": command not found")
 }
