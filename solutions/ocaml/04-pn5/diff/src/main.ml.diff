@@ -1,7 +1,9 @@
 let () =
   while true do
     print_string "$ ";
     flush stdout;
     let command = input_line stdin in
+    if command = "exit" then
+      exit 0;
     print_endline (command ^ ": command not found")
   done
